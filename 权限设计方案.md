# 容量管理平台 权限设计方案

> 版本：v4.0
> 目标：仪表盘、快照、数据源的**实例级**权限控制；请求耗时 < 1s
> 约束：外部用户权限库**不能直连 SQL**，只能通过其 HTTP API 获取组织信息

---

## 1. 背景与职责边界

本平台是大系统的一个子平台。用户/部门/团队数据存放在外部用户权限库，**仅能通过外部权限服务提供的 HTTP API 访问，不能直连数据库查询**。外部 RBAC 服务控制接口层（method+path+角色），**实例级权限由本平台负责**。

| 事项 | 负责方 |
| ---- | ---- |
| 用户/部门/团队数据 | 外部用户权限库（本平台**仅通过 API 访问**） |
| 认证登录、接口级权限 | 外部 RBAC 服务 / 网关 |
| **实例级权限**（资源归属、分享、可见范围） | **本平台** |
| 资源归属(owner_id)、分享(resource_shares) | 本平台 |

### 已确认决策

| 决策项 | 结论 |
| ---- | ---- |
| 认证方式 | 暂不做登录，请求头固定身份，后续接外部 RBAC |
| 组织数据获取 | **外部权限服务 API**（网关注入 / 本平台调用），不落库 |
| 分享粒度 | 按人 + 按团队 |
| 管理员 | 需要（全量可见可管） |

---

## 2. 总体架构

```
┌────────────────────────────────────────────────────────────┐
│ 前端                                                            │
│ 请求头携带身份：X-User-Id（开发期前端配置固定身份）              │
└──────────────────────┬─────────────────────────────────────┘
                       │
┌──────────────────────▼─────────────────────────────────────┐
│ 外部 RBAC 服务 / 网关（接口层控制）                            │
│ · 认证、method+path+角色校验                                  │
│ · 注入身份头：X-User-Id / X-User-Role / X-User-Dept /        │
│   X-User-Teams / X-Visible-User-Ids（网关能力允许时）         │
└──────────────────────┬─────────────────────────────────────┘
                       │ ① 身份头解析（最快）
┌──────────────────────▼─────────────────────────────────────┐
│ 本平台（实例层控制）                                            │
│                                                             │
│ IdentityProvider（可插拔）                                    │
│  · HeaderProvider       ：解析网关身份头（0 额外请求）         │
│  · ExternalAPIProvider  ：调外部权限服务 API + 内存缓存        │
│  · DevProvider          ：开发模拟身份                        │
│                                                             │
│ ① 身份中间件 → UserContext（缓存）                            │
│ ② 可见范围 → 成员ID集合（缓存）+ 分享表                        │
│ ③ 可见性 SQL：owner_id IN (成员集合) OR 分享                  │
│ ④ 写操作校验：owner_id / 分享 / admin                        │
└────────────────────────────────────────────────────────────┘
```

**核心思路**：组织关系完全留在外部，本平台通过**外部 API + 内存缓存**拿到"当前用户可见的成员用户 ID 集合"（团队成员/部门成员），再做 `owner_id IN (...)` 过滤。分享关系是本平台实例数据，自维护。

---

## 3. 身份与可见范围获取

### 3.1 UserContext

```go
type UserContext struct {
    UserID   string    // 外部用户ID（= owner_id）
    Role     string    // admin / manager / team_leader / member
    DeptID   string    // 所属部门ID
    TeamIDs  []string  // 所属团队ID集合（分享给团队判定用）
    // 可见用户ID集合：可查看其仪表盘/快照的用户
    // = 自己 ∪ 团队成员(团队长) ∪ 部门所有团队成员(部长) ∪ (admin: 全量)
    VisibleUserIDs []string
}
```

### 3.2 获取方式（可插拔，按部署环境选择）

| 实现 | 数据来源 | 额外开销 | 适用场景 |
| ---- | ---- | ---- | ---- |
| `HeaderProvider` | 网关注入身份头（含 VisibleUserIDs） | 0 | 生产推荐，网关能力允许时 |
| `ExternalAPIProvider` | 调用外部权限服务 API（§4） | 1 次调用 + 缓存 | 网关只注入 user_id 时 |
| `DevProvider` | 配置文件指定模拟身份 | 0 | 开发调试/多角色联调 |

### 3.3 获取流程

```
请求头 X-User-Id
  → 内存缓存命中？ → 用缓存的 UserContext
  → 否 → HeaderProvider 解析身份头
       （或 ExternalAPIProvider 调外部 API 组装 UserContext）
       → 写入缓存（TTL 5 分钟）
```

> 缓存为纯内存（go-cache），进程重启重建，不落库。

---

## 4. 外部权限服务 API 约定（需外部配合）

> 本方案依赖以下外部接口，**请与外部权限服务方确认接口路径与字段**，以实际为准；adapter 层统一适配。

| 接口 | 说明 | 用途 |
| ---- | ---- | ---- |
| `GET /api/user/permission?user_id={id}` | 返回用户：角色、部门ID、团队ID集合 | 组装 UserContext |
| `GET /api/user/visible-users?user_id={id}` | 返回该用户可见的用户ID集合（团队长→本团队、部长→本部门，由外部计算） | 组装 VisibleUserIDs（**首选**，本平台零推导） |
| `GET /api/user/team-members?team_id={id}` | 返回团队成员用户ID列表 | 备选：本平台按角色推导可见范围 |
| `GET /api/user/dept-members?dept_id={id}` | 返回部门下所有成员用户ID列表 | 备选：部长可见范围 |
| `GET /api/user/search?q={kw}` | 用户搜索 | 分享弹窗 |
| `GET /api/team/list?dept_id={id}` | 团队列表 | 分享弹窗（按团队分享） |

**两种可见范围模式（外部能力决定）**
- **模式 A（推荐）**：外部直接提供 `visible-users` 接口 → 本平台拿到现成的可见用户集合，零推导、最简单；
- **模式 B（通用）**：外部仅提供 `team-members` / `dept-members` → 本平台根据角色自行组装：member=[自己]、team_leader=自己+团队成员、manager=自己+部门成员。

> 请求方身份：本平台调用外部 API 时携带网关注入的 token（透传）或服务间固定凭证，由外部校验。

---

## 5. 数据模型

### 5.1 本平台新增表

```sql
-- 分享表（仪表盘/快照通用）
CREATE TABLE resource_shares (
  id             BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  resource_type  VARCHAR(16) NOT NULL,          -- dashboard / snapshot
  resource_id    VARCHAR(64) NOT NULL,
  share_to_type  VARCHAR(8)  NOT NULL,          -- user / team
  share_to_id    VARCHAR(64) NOT NULL,          -- 外部用户ID / 外部团队ID
  can_edit       TINYINT(1) DEFAULT 0,
  shared_by      VARCHAR(64) NOT NULL,
  created_at     DATETIME(3), updated_at DATETIME(3), deleted_at DATETIME(3),
  UNIQUE KEY uk_share (resource_type, resource_id, share_to_type, share_to_id),
  KEY idx_share_to (share_to_type, share_to_id)
);
```

### 5.2 存量表加字段

```sql
ALTER TABLE dashboards  ADD COLUMN owner_id VARCHAR(64) NOT NULL DEFAULT '' AFTER id;
ALTER TABLE snapshots   ADD COLUMN owner_id VARCHAR(64) NOT NULL DEFAULT '' AFTER id;
ALTER TABLE datasources ADD COLUMN owner_id VARCHAR(64) NOT NULL DEFAULT '' AFTER id;

ALTER TABLE dashboards  ADD KEY idx_dashboards_owner (owner_id);
ALTER TABLE snapshots   ADD KEY idx_snapshots_owner (owner_id);
ALTER TABLE datasources ADD KEY idx_datasources_owner (owner_id);
```

> `owner_id` 存外部用户 ID；存量数据迁移补齐（缺失归管理员）。

---

## 6. 权限矩阵

### 6.1 仪表盘 / 快照（权限同上）

| 操作 | admin | manager（部长） | team_leader（团队长） | member |
| ---- | ---- | ---- | ---- | ---- |
| 查看 | 全部 | 本部门所有团队的 | 本团队所有成员的 | 自己的 + 分享给我的/我团队的 |
| 创建 | 可 | 可 | 可 | 可 |
| 编辑/删除 | 全部 | 自己的 + 分享且可编辑 | 同左 | 同左 |
| 分享 | 可 | 仅自己拥有的 | 同左 | 同左 |

### 6.2 数据源

| 操作 | admin | 其他 |
| ---- | ---- | ---- |
| 查看/编辑/删除 | 全部 | 仅自己创建的 |

### 6.3 判定函数

```go
// view / edit / delete / share
func (s *Server) checkResourcePerm(ctx *gin.Context, resType, resID, perm string) (bool, error)
```
- view：资源在可见范围内（§7）；
- edit/delete/share：`owner_id == me || role == admin || 分享(can_edit=1 且 share_to=me)`；
- 未通过返回 `403 {"errorCode":"40300","errorMessage":"无权限"}`。

---

## 7. 可见性判定（性能核心）

### 7.1 列表可见范围（一条 SQL）

```sql
-- :me 当前用户ID, :role 角色, :team_ids 我的团队集合, :visible_ids 可见用户集合
SELECT d.*
FROM dashboards d
WHERE d.deleted_at IS NULL
  AND (
    :role = 'admin'                                   -- 1. 管理员全量
    OR d.owner_id = :me                               -- 2. 自己的
    OR d.owner_id IN (:visible_ids)                   -- 3. 团队/部门成员（外部API+缓存）
    OR d.id IN (                                      -- 4. 分享给我的/我团队的
        SELECT rs.resource_id FROM resource_shares rs
        WHERE rs.resource_type = 'dashboard' AND rs.deleted_at IS NULL
          AND ((rs.share_to_type='user' AND rs.share_to_id=:me)
            OR (rs.share_to_type='team' AND rs.share_to_id IN (:team_ids)))
    )
  )
ORDER BY d.created_at DESC
LIMIT :limit OFFSET :offset
```

- member：`visible_ids=[自己]` → 命中 2/4；team_leader：含团队成员；manager：含部门成员；admin 短路；
- 全部走索引：`d.owner_id`、`resource_shares(share_to_type, share_to_id)`；
- `visible_ids` 来自缓存（外部 API 结果），几千 ID 的 IN 列表走索引无压力。

### 7.2 详情/编辑

- 查资源 → `checkResourcePerm` 校验；
- 数据源：`WHERE id = :id AND owner_id = :me`（admin 免过滤）。

---

## 8. 分享机制

| 接口 | 说明 |
| ---- | ---- |
| `POST /api/v1/dashboards/share` | 添加/更新 `{resource_type, resource_id, share_to_type, share_to_id, can_edit}` |
| `POST /api/v1/dashboards/share/remove` | 取消分享 |
| `GET /api/v1/dashboards/share/list?resource_id=` | 分享列表 |
| `GET /api/v1/users/search?q=` | 用户搜索（转发外部 §4） |
| `GET /api/v1/teams/list` | 团队列表（转发外部 §4） |

- 仅资源拥有者或 admin 可分享；
- 按人 / 按团队（团队判定用 UserContext.TeamIDs）；
- 默认只读；用户/团队搜索转发外部 API，不落库。

---

## 9. 性能方案（< 1s）

| 环节 | 预算 |
| ---- | ---- |
| 身份中间件（缓存命中） | < 5ms |
| 可见用户集合（缓存命中） | < 5ms |
| 可见性 SQL（索引 + 分页） | < 100ms |
| 面板数据查询（既有逻辑） | < 500ms |
| 网络/序列化 | < 100ms |

- **索引**：owner_id ×3、resource_shares uk_share + share_to；
- **缓存（内存，不落库）**：
  - UserContext（含 VisibleUserIDs / TeamIDs）：TTL 5 分钟；
  - 用户/团队搜索：TTL 1 分钟；
- **原则**：权限过滤融入一条 SQL，禁止逐条代码过滤；
- **兜底**：缓存失效瞬间的外部 API 调用有超时（2s）+ 失败时返回空可见集（不阻塞请求）。

---

## 10. 改造清单

### 后端
| 模块 | 改动 |
| ---- | ---- |
| 新增 `identity/` | IdentityProvider 接口 + Header/ExternalAPI/Dev 实现 + UserContext 缓存 |
| 新增 `permission/` | checkResourcePerm、可见范围 SQL 构建器 |
| 新增 `external/` | 外部权限服务 API client（§4 接口适配） |
| 新增 | 分享接口 + resource_shares 表 |
| 改造 `dashboards` | 列表可见性过滤；增删改权限校验 |
| 改造 `snapshots` | 列表可见性过滤；增删权限校验 |
| 改造 `datasources` | 按 owner_id 过滤（admin 除外） |
| 新增 | 用户/团队搜索（转发外部） |

### 前端
| 页面 | 改动 |
| ---- | ---- |
| 全局 | 请求头携带 X-User-Id（开发期配置固定身份） |
| BrowsePage | 我的 / 分享给我的 / 团队 三个分组对接；只读标识 |
| 仪表板详情 | 无编辑权限时隐藏保存/编辑等（只读模式） |
| 仪表板菜单 | 新增"分享"弹窗 |
| DataSourcesPage | 只显示自己的；编辑/删除仅对自己的 |
| 快照列表 | 按可见性显示；分享入口 |

---

## 11. 实施步骤

1. **一期（最小可用）**：identity 模块（DevProvider + HeaderProvider 解析）+ 数据源/仪表板/快照加 owner_id + 可见性过滤 + 写操作 403 + 前端只读模式与三个分组。
2. **二期**：resource_shares 分享接口 + 分享弹窗；admin 全量；用户/团队搜索转发。
3. **三期**：对接外部权限服务（ExternalAPIProvider），确认 §4 API 清单，接入网关身份头。

---

## 12. 风险与注意事项

| 风险 | 应对 |
| ---- | ---- |
| 外部 API 未就绪 | 一期用 DevProvider/HeaderProvider 先行，外部 API 三期接入 |
| 外部 API 延迟 | 2s 超时 + 内存缓存 + 失败返回空可见集（不阻塞） |
| 身份头伪造 | 生产由网关校验注入；无头请求拒绝或匿名（无可见资源） |
| 外部计算可见集合模式 A 不可用 | 降级模式 B：本平台按角色调 team-members/dept-members 组装 |
| 组织变更生效延迟 | 5 分钟 TTL；预留外部通知清缓存 hook |
| 存量数据无归属 | 迁移脚本补齐 owner_id，缺失归管理员 |
| 分享列表膨胀 | uk_share 防重复；按资源分页 |
| 1s 性能 | 可见范围全部走缓存 + 索引 SQL；外部 API 仅在缓存 miss 时调用 |
